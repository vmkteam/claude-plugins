# {TASK_ID} — Спецификация

## Summary
{1-2 предложения что делаем}

## Шаги реализации
1. [ ] {конкретное действие — атомарное, проверяемое}
2. [ ] {следующее действие}
3. [ ] ...

Пример для задачи с изменением схемы БД (стадия Dev+):
1. [ ] Изменить схему в `docs/{name}.pgd` (/pgd) или `docs/{name}.pdd` (/pgmdd)
2. [ ] `pgdesigner diff old.pgd new.pgd` → миграция в `docs/patches/`
3. [ ] `pgdesigner generate` → обновить `docs/{name}.sql` (всегда актуальный)
4. [ ] `pgmigrator run` → `make mfd-xml` → `make mfd-model` → `make mfd-repo NS={ns}`
5. [ ] Если нужны поиски/фильтрация — добавить Search через /mfd (SearchObject в mfd-xml, `make mfd-repo NS={ns}`)
6. [ ] Добавить бизнес-логику в `pkg/{domain}/{file}.go`
7. [ ] Добавить RPC-метод в `pkg/rpc/{file}.go` с zenrpc-аннотациями
8. [ ] `make generate`
9. [ ] Написать тесты

> **Работа с БД — какой скилл использовать:**
> - /pgd — формат pgDesigner (.pgd), редактирование схемы
> - /pgmdd — формат MicroOLAP Database Designer (.pdd)
> - /mfd — генерация Go-кода из схемы: модели, репозитории, поиски (SearchObject). Стандартные поиски/фильтрация всегда добавляются через mfd

Пример для бага:
1. [ ] Исправить `pkg/rpc/order.go:142` — nil check перед обращением к pointer
2. [ ] Добавить тест на этот edge case
3. [ ] `make generate` (если менялись аннотации)

## Примеры кода (скелеты)

Показать ключевые фрагменты **реального кода задачи** — сигнатуры, модели, конвертеры, RPC-методы.
Пользователь проверяет архитектуру до написания кода. Не копировать абстрактные примеры.

```go
// Реальные структуры, конвертеры (colgen MapP), RPC-методы (zenrpc-аннотации)
```

## Миграции (включать только если есть SQL-изменения)
- Стадия: {Идея — нет миграций, правим sql напрямую / Dev+ — миграция через pgdesigner diff}
- [ ] Схема обновлена, `docs/{name}.sql` актуален
- [ ] Миграция в `docs/patches/` (нет блокирующих ALTER, `CREATE INDEX CONCURRENTLY` → `-NONTR.sql`)
- План отката: {описание или "не требуется"}

## API compatibility (включать только если меняются RPC-методы)
- [ ] Нет breaking changes для существующих клиентов
- [ ] Новые параметры опциональны или с default

## Pre-review checklist
- [ ] Конвертеры между слоями через colgen (MapP/Map для каждого нового типа)
- [ ] `make generate` чистый (нет неожиданных diff в *_zenrpc.go, *_colgen.go)
- [ ] Authz: проверка прав внутри методов, не только middleware (IDOR)
- [ ] Errors: `NewStringError` для клиента, `newInternalError` для Sentry
- [ ] Логирование: достаточно для диагностики нового функционала
- [ ] Тесты: happy path + основные error cases

## Стиль кода
- Dave Cheney: ясные зависимости, никакой магии, каждая функция понятна без контекста
- Rob Pike: три строки простого кода лучше одной умной, никаких абстракций ради абстракций
- Не добавляй то, что не просили. Не рефактори соседний код. Не добавляй "на будущее"

## Критерии готовности
- [ ] Тесты проходят
- [ ] make lint чисто
- [ ] Pre-review checklist пройден
- [ ] /go-review без блокеров
